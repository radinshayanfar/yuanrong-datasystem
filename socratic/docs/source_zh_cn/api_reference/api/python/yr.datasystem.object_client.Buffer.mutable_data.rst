yr.datasystem.object_client.Buffer.mutable_data
===============================================

.. py:method:: yr.datasystem.object_client.Buffer.mutable_data

    获取Buffer的可读写 `memoryview`。

    返回：
        memoryview，Buffer的可读写 `memoryview`。
