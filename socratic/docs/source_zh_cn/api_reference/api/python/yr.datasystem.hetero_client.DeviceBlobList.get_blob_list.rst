yr.datasystem.hetero_client.DeviceBlobList.get_blob_list
========================================================

.. py:class:: yr.datasystem.hetero_client.DeviceBlobList.get_blob_list(blob)

    获取 :class:`yr.datasystem.hetero_client.Blob` 列表。

    输出：
        blob(list)：返回 :class:`yr.datasystem.hetero_client.Blob` 列表。

